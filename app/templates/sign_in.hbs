<h2>Sign In</h2>

<form {{action "sign_in" on="submit"}}>
  {{input type=text value=email name="email" placeholder="Email"}}
  {{input type=password value=password name="password" placeholder="Password"}}
  <button type="submit">Sign In</button>
</form>